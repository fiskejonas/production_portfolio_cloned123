---
---

<a
  target="_blank"
  rel="noopener noreferrer"
  id="view-selected-cards"
  type="button"
  class="appearance-none z-50 fixed w-xl text-xs bottom-[15%] hidden right-[10%] knap py-xs px-sm bg-positive100 text-primary100 shadow:lg font-semibold rounded-lg hover:bg-purple-700 focus:outline-none focus:ring-2 focus:ring-purple-400 focus:ring-opacity-75"
>
  <slot />
</a>

<style>
  .knap {
    transition: 750ms cubic-bezier(0.075, 0.82, 0.165, 1);
    offset-anchor: left bottom;
  }

  .knap:hover {
    cursor: pointer;
    transform: translateY(-10px);
    transition: 200ms cubic-bezier(0.165, 0.84, 0.44, 1);
  }

  .knap:active {
    transform: translateY(0px);
    scale: 0.95;
    transition: 200ms cubic-bezier(0.165, 0.84, 0.44, 1);
  }
</style>
